# Project Memory

- [Solver refactor plan](solver-refactor-plan.md) — cube=geometry / solver=search split. Stages 1-3 DONE 2026-08-07 (TRubikSolver extraction; Coherent→solver + Floor→TRubikSolver static + ExecuteGA returns gens; "search state leaves cube" = target+seeds+TCluster.Cubies→solver, GPU single-param). All behavior-preserving (2^4 batch identical). Remaining: trajectory-as-log (unify interactive+batch); revive Solutions via a solver copy ctor; optional cleanups (vestigial seed-build in ScoreCube path — would re-baseline).

- [GLSL macro name collisions](glsl-macro-name-collisions.md) — never name a shader local after an uppercase Variables macro (N, SIZE, ...); it expands to an integer and breaks with misleading errors.
- [Greedy diversity research](greedy-diversity-research.md) — mixed-Givens `mode` is the macro-move generator (+310% vs baseline); findings in Docs/Rozklad_greedy_roznorodnosc.md, tools in TGreedyDiversity.cs.
- [Seeder role boundary](seeder-role-boundary.md) — the seed generator only seeds; don't curate/filter the pool, selection is the GA's job.
- [Project purpose & scope](project-purpose-scope.md) — pure uniform GA as a teaching/imagination artifact, not a fast solver; no endgame special-casing, no hybrids; improve via GA's own levers, reason before batching.
- [Pace: explain before code](pace-explain-before-code.md) — user loses track with fast/bulk code dumps; explain what+why in plain terms first, small single-focus edits, keep them in the loop.
- [GA mechanism](ga-mechanism.md) — GA focuses on ONE cubie per short (~50-gen) run, solve-it-without-breaking-others; on failure retries a DIFFERENT cubie (greedy peel).
- [Measure tail, not mean](measure-tail-not-mean.md) — Las Vegas solver: judge by tail/variance not median; SEED_RATIO=100% kills the mono-twist tail; endgame is a PLATEAU, so STALL's escape becomes a SIDEWAYS move GATED by the limit (`< || (Stall>=limit && <=)`) — always-on `<=` thrashed; STALL stays as the timer, not worse-accept.
- [Cluster orbit structure](cluster-orbit-structure.md) — full-orientation cluster exists at even Size=2(N-1) (4^3, 6^4, not 2N-1); chirality bit IS now in TCubie.Index (2026-08-01) so ClusterIndex splits chiral orbits → every cluster is a single orbit ≤ N!·2^(N-1); host injects MAX_CLUSTER to size the coherence scratch.
- [Coherence eval solution](coherence-eval-solution.md) — current best = `2*P*S` ladder, GATEWAY G+0 (single G-block = one move from solved) as the closing target, pow2 pre-guard + full-block(agreeAxes=0) exclusion; NO E (E chased G+G, which is PAST the gateway — backwards). 3^3: 5 batches ~168 mean, worst tail 527/50, var <20k (~4×/7×/70× over coupling).
- [Build workflow](build-workflow.md) — user builds/runs in Visual Studio; don't run `dotnet build` to verify, just save edits.
- [FreeMoves bug invalidates measurements](freemoves-bug-invalidates-measurements.md) — long-standing no-op/dedup bug in GetFreeMoves contaminated ALL pre-fix metric batches; treat old numbers as void, re-baseline from scratch.
- [Endgame tail is findability](endgame-tail-is-findability.md) — coherence ladder is a richer gradient than count-peel (has the escape route); the tail is finding the merge/commutator, not the metric — lever is endgame seeds. Includes the fine-fitness + structure-accept architecture.
- [2^5 first solve](2x5-first-solve.md) — HOLY GRAIL: 2^5 solved 2026-08-06 (~180k GA). Params: seeds 0%, pop 2048, Floor 4, `*= Floor/scrambled` below floor, metric P2+N*S, ladder-sideways coherent accept. Bottleneck: first rung 1+1+1+1 ate ~159k of 180k (~88%) — the flat first-merge is THE next lever.
- [Unified evaluator](unified-evaluator-direction.md) — COMPLETE 2026-08-04 (milestone): ONE evaluator for single-cluster (2^4/2^5) AND multi-cluster (6^3), no Micro/Macro or size split. Holds only active cluster (MAX_CLUSTER); ALWAYS scans every prefix (best-of-prefixes beats single-eval: 2^4 var 124k vs 3.25M); Solved break = precomputed per-prefix `solvedMask` bitmask (per-cubie forward replay, O(countSolved*GENES)), which resolved the commutator-restore trap and made scan affordable multi-cluster. Peel-proxy fixes the endgame baseline (ScoreLadder=0). Don't reintroduce single-eval. NOTE: 2^4 result gains are from population 4096, not code.
